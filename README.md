# DigitalPathology_CNN-SVM

Задача решалась с помощью сверточной сети SimpleCNN + SVM по эмбеддингам (ансамбль CNN+SVM). CNN обучается с аугментациями, валидацией и early stopping, затем по 512-мерным эмбеддингам обучается SVM с RBF-ядром. При тесте используется усреднение вероятностей двух моделей.

---

## Результаты

Обучение на `train`, проверка на `test`:
   - Точность на части `test`: 0.9889
   - Точность на `test`: 0.9596
   - Точность на `test_tiny`: 0.9556

---

## Реализованные дополнительные опции

1. Валидация на части обучающей выборки (#LBL_VAL) — train/val split в методе `Model.train`.
3. Автоматическое сохранение модели при обучении (#LBL_SAVE) — метод `Model.save`.
4. Загрузка модели из облачного хранилища в `load()` (#LBL_LOAD1, #LBL_LOAD2).
5. Вывод функции потерь и accuracy по эпохам (#LBL_LOG).
6. Построение графиков обучения (loss/accuracy) по `model.history` (#LBL_PLOTS).
7. Автоматическое тестирование на тестовом наборе через `test_on_dataset` (#LBL_TEST).
11. Аугментации изображений (флипы, повороты, ColorJitter) в `HistologyTorchDataset` (#LBL_AUG).
